// Урок 1
$(document).ready(function(){
  // СЕЛЕКТОРЫ

  // Базовые селекторы (#id, tagName, .class)
  // $('p.blue').css('border', 'solid 3px red')

  // Селекторы взаимодействия (parent() kodida butun conteiner ramkalanadi, child, h2+p qisa + da 1ta ramkalanadi, 
  // ~ hammasi ramkalanadi h2 tegidi и т.д.);
  // $('h2').css('border', 'solid 3px red');

  //Простые фильтры (:first tepa, :last past, even 1 tawab tawab iwlidi 1, 3, 5; odd 2, 4, 6; eqda ozimiz belgilase 
  //boladi eq(1) qilip и т.д.)
  //$('p:eq(3)').css('border', 'solid 3px red');

  //Фильтры по содержимому (:hasda h2 belgilase h2 bittasida span bor :has(ichiga) span dip yozse ikkala h2ni mas faqat 
  //spani  bor h2ni korsitadi, :parentda box ichida qanaqadur element borlilari belgilanadi, :emptyda hich qanaqa
  //element yogi korinadi и т.д.)
  //$('.box:empty').css('border', 'solid 3px red');

  //Фильтры по атрибутам ([name ~= value]^ bu belgida luboy bitta a tegidan http ni opqoyse kegin ^ yozse
  // uwa httpsi yo sozzi tepasidi hammasi ramkada boladi ,! da bu yozgan silkamizani tegidi hammasi ramkada boladi
  // ,$ bunda .ru bilan tugidigan hammasi ramkada boladi  и т.д)
  //$('a[href$=".ru"]').css('border', 'solid 3px red');

});
